import SwiftUI

struct RecetaView: View{
    var body: some View{
        VStack{
            Text("Bienvenido a tu espacio Recetas")
                .font(.headline)
                .padding()
            Text("Progreso de peso")
                .font(.subheadline)
        }
        .padding()
    }}
